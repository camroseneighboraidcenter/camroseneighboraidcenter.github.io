{
  "private": true,
  "type": "module",
  "name": "camroseneighboraidcenter.ca",
  "description": "Camrose Neighbor Aid Center website",
  "version": "0.0.1",
  "scripts": {
    "lint": "prettier --check . && astro check && eslint \"src/**/*.{js,jsx,ts,tsx,astro}\" && stylelint \"**/*.{css,astro}\" --report-needless-disables --report-invalid-scope-disables && markdownlint \"**/*.md\" \"**/*.mdx\"",
    "fix": "prettier --write . && astro check && eslint --fix \"src/**/*.{js,jsx,ts,tsx,astro}\" && stylelint \"**/*.{css,astro}\" --fix --report-needless-disables --report-invalid-scope-disables && markdownlint --fix \"**/*.md\" \"**/*.mdx\"",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npm run build && npx playwright test",
    "test:a11y": "npm run build && npx playwright test tests/accessibility.spec.ts",
    "test:perf": "npm run build && npx playwright test tests/performance.spec.ts",
    "test:seo": "npm run build && npx playwright test tests/seo.spec.ts",
    "test:lighthouse": "npm run build && npx lhci autorun",
    "clean": "rm -rf test-results playwright-report .lighthouseci dist",
    "test:all": "npm run build && npm run test:lighthouse && npx playwright test",
    "test:ci": "npm run build && npx playwright test --project=chromium-light --project=chromium-dark",
    "test:report": "npx playwright show-report playwright-report",
    "test:batched": "node scripts/run-tests.js"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.7",
    "@astrojs/mdx": "^5.0.0",
    "@astrojs/sitemap": "^3.7.1",
    "astro": "^6.0.4",
    "remark-attr": "^0.11.1",
    "sharp": "^0.34.5",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@eslint/js": "^10.0.1",
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.58.2",
    "@types/hast": "^3.0.4",
    "@typescript-eslint/parser": "^8.57.0",
    "axe-core": "^4.11.1",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-astro": "^1.6.0",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "lighthouse": "^13.0.3",
    "markdownlint": "^0.40.0",
    "markdownlint-cli": "^0.48.0",
    "playwright": "^1.58.2",
    "postcss-html": "^1.8.1",
    "prettier": "^3.8.1",
    "stylelint": "^17.4.0",
    "stylelint-config-standard": "^40.0.0",
    "terser": "^5.46.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.0"
  }
}
